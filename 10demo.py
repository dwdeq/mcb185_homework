import math

print('hello, again') # greeting

print(1.5e-2)

def circle_area(r): return math.pi * r**2
def rectangle_area(w, h): return w * h
def cylinder_volume (r, w, h): return circle_area(r) * rectangle_area(w, h) 